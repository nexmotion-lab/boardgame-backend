spring:
  datasource:
    url: jdbc:mysql://localhost:3306/boardgame
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  servlet:
    session:
      timeout: 15m
